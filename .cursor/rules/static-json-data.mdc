---
description: Datos fake con fetch seed (src/data/db) + form/localStorage para grillas en Pages
globs: "{src/pug/components/**/*.pug,src/js/modules/**/*.js,src/js/db/**/*.js,src/data/**/*.json}"
alwaysApply: false
---

# JSON estático + form + grilla (simulación)

Patrón del boilerplate para demos con **seed en JSON** y, si hay form, **mismo código en dev y GitHub Pages**.

Estilos con **Tailwind 4**. Sin Sass ni partials SCSS.

## Dos modos (elige uno según el ejercicio)

### A) Solo lectura (grilla)

```
src/data/db/*.json → public/data/db/ → fetch → UI
```

### B) CRUD form + grilla (persona-grid / tutor-grid)

```
1. fetch seed → public/data/db/*.json (si no hay localStorage)
2. localStorage[storageKey] → lista con id por registro
3. Crear / Actualizar / Eliminar → write localStorage + re-render
```

- Cada registro tiene `id` (se genera al seed o al crear).
- Create: Submit en modo crear.
- Read: grilla de cards.
- Update: Editar rellena el form; Submit guarda.
- Delete: botón en card + confirm.
- Mismo código en dev y GitHub Pages (por navegador).

## Referencia canónica

| Pieza | Ruta |
|-------|------|
| Seed (tabla) | `src/data/db/persona.json` → `{ "personas": [ … ] }` |
| Config UI | `src/data/persona-grid-data.json` |
| Markup | `src/pug/components/persona-grid.pug` |
| JS UI | `src/js/modules/personaGrid.js` |
| Store / “BD” | `src/js/db/crudDemoStore.js` |

Gulp: `assets` copia `src/data/**/*.json` → `public/data/`. Los `*-data.json` se inyectan a Pug (rule `pug-data-files`).

## Comportamiento JS (modo B — CRUD)

1. Si existe `localStorage[storageKey]` → usarlo (asignar `id` si falta).
2. Si no → `fetch(dataUrl)` desde `./data/db/…`, normalizar con `id`, guardar storage, pintar grilla.
3. **Create** → append + `id` nuevo.
4. **Update** → Editar carga form; Submit reemplaza por `id`.
5. **Delete** → confirm + filtra por `id`.
6. Sin API Gulp de escritura ni download.

## Límites

| | Dev / Pages |
|--|-------------|
| Ver seed + grilla | Sí |
| Submit actualiza grilla | Sí (este browser) |
| Otros visitantes ven lo que escribiste | No (cada browser tiene su localStorage) |
| Escribe `src/data/db/persona.json` en el repo | No |

## Reglas

1. Skill `create-static-json-module` para módulos UI nuevos de este patrón.
2. Seeds de tablas → `src/data/db/`; lógica store/JOIN → `src/js/db/` (rule + skill `emulated-db`).
3. `data-url` + `data-storage-key` en la raíz del módulo.
4. Markup: form arriba, grilla debajo (`aria-live` en la grilla).
5. Anclas JS con clases estables (`persona-grid__*`); presentación con Tailwind y tokens `:root`.
6. Registrar el módulo en `src/js/index.js` → `initComponents`.
